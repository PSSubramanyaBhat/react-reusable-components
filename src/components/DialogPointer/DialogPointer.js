import React from 'react';
import PropTypes from 'prop-types';
import styles from './DialogPointer.module.css';

const DialogPointer = props => {
return (
<div className={styles.DialogPointer }>
</div>
);
};


DialogPointer.propTypes = {

};

export default DialogPointer;